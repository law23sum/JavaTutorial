package src.main.classs.field;

public class FieldTypes {
    void booleanField() {
        boolean innerStatus = true;
        status = innerStatus;
    }

    void hideJunior() {
        String exchange = "interact";
        personal = exchange;
    }

    void constValue() {
        int calculation = (int) ((UNCHANGEABLE / (1 - UNCHANGEABLE)) - (1 / (1 + TRANSCENDENT)));
    }

    private boolean status = false;
    private static String personal = "hidden";
    protected final int UNCHANGEABLE = 42;
    protected char statute = 's';
    public static float repeatable = 1 / 3;
    public final double TRANSCENDENT = Math.sqrt(2);
}
